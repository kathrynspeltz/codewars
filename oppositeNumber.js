/* Very simple, given a number (integer / decimal / both depending on the language), find its opposite */

function opposite(number) {
    return -number
}

console.log(opposite(1))
console.log(opposite(-12))
console.log(opposite(6))